applications:
- services:
  - Question and Answer-v3
  - New Relic-x2
  - SendGrid-6w
  - Redis Cloud-zr
  - ClearDB MySQL Database-81
  disk_quota: 1024M
  host: karoshi
  name: karoshi
  command: bundle exec rake db:migrate && rake db:create && rake db:schema:load && rake db:seed && rails s -p $PORT
  path: .
  domain: mybluemix.net
  instances: 1
  memory: 256M
