<p id="notice"><%= notice %></p>
<section id="sidebar">
  <h1><%=@topic.subject%></h1>
  <% unless @topic.status %>
    <section id="join_topic">
      <%= render 'group_topic' %>
    </section>
  <% end %>
  <%= render 'topic_groups' %>
</section>

<section id="topic_posts">
  <%= render 'topic_posts' %>
</section>
