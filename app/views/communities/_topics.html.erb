<header><h1><%= t "community.topics"%></h1></header>
<% topics = @community.topics.search('',params[:tpage]) %>
<ul>
<% topics.reverse.each do |topic| %>
  <li>
  	<%= link_to topic.subject,topic %>
  </li>
<% end %>
</ul>
<%= will_paginate topics,param_name:'tpage' %>
