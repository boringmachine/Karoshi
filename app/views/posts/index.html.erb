<section id="posts">

  <section id="new_post_section">
  	<% @post = Post.new %>
    <%= render 'form' %>
  </section>

  <%= render 'list_posts' %>

  <% if GroupUser.groups(current_user.id).count == 1 %>
     <%= render 'explain'%>
  <% end %>
  
  <% if @posts.count == Post.per_page %>
     <%= render 'infbtn' %>
  <% end %>

</section>

