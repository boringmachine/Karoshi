<%= form_for(@community_user) do |f| %>
  <% if @community_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@community_user.errors.count, "error") %> prohibited this community_user from being saved:</h2>

      <ul>
      <% @community_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :community_id %><br />
    <%= f.number_field :community_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
