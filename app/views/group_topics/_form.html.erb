<%= form_for(@group_topic) do |f| %>
  <% if @group_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_topic.errors.count, "error") %> prohibited this group_topic from being saved:</h2>

      <ul>
      <% @group_topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_id %><br />
	<%= collection_select(:group_topic, :group_id, Group.owngroups(current_user.id) , :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :topic_id %><br />
	<%= collection_select(:group_topic, :topic_id, Topic.all, :id, :subject) %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
